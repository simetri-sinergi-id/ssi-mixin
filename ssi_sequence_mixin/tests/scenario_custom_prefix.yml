scenario:
  sequence:
    name: "Test Sequence Prefix"
    code: "ssi.test.sequence"
    number_next: 1
    number_increment: 1
    padding: 6
  template:
    name: "Template with Prefix"
    initial_string: "/"
    computation_method: "use_python"
    python_code: "result = document.value > 10"
    sequence_selection_method: "use_sequence"
    add_custom_prefix: True
    prefix_python_code: "result = 'TEST-PREFIX/'"
    sequence: 1000
  test_model:
    name: "/"
    value: 15
